<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:Golyath.UI.ViewModels.Workout"
             x:Class="Golyath.UI.Views.Workout.ActiveWorkoutPage"
             x:DataType="vm:ActiveWorkoutViewModel"
             BackgroundColor="{StaticResource AppDarkBg}"
             NavigationPage.HasNavigationBar="False"
             Shell.NavBarIsVisible="False">

    <Grid RowDefinitions="Auto,*,Auto">

        <!-- ── Header ── -->
        <Grid Grid.Row="0" RowDefinitions="Auto,Auto,1">
            <!-- Title + buttons row -->
            <Grid Grid.Row="0"
                  ColumnDefinitions="Auto,*,Auto"
                  Padding="16,12"
                  BackgroundColor="{StaticResource AppDarkSurface}">
                <Label Grid.Column="0"
                       Text="&#xE5CB;"
                       FontFamily="MaterialIcons"
                       FontSize="28"
                       TextColor="White"
                       VerticalOptions="Center"
                       Margin="0,0,8,0">
                    <Label.GestureRecognizers>
                        <TapGestureRecognizer Tapped="OnBackClicked" />
                    </Label.GestureRecognizers>
                </Label>
                <VerticalStackLayout Grid.Column="1" Spacing="2" VerticalOptions="Center">
                    <Label Text="{Binding WorkoutTitle}"
                           TextColor="White"
                           FontSize="18"
                           FontAttributes="Bold" />
                    <Label Text="{Binding ElapsedTime}"
                           TextColor="{StaticResource Accent}"
                           FontSize="14"
                           FontFamily="OpenSansRegular" />
                </VerticalStackLayout>
                <HorizontalStackLayout Grid.Column="2" Spacing="8" VerticalOptions="Center">
                    <Button Text="Discard"
                            Command="{Binding AbandonWorkoutCommand}"
                            BackgroundColor="Transparent"
                            TextColor="#888888"
                            FontSize="13"
                            Padding="8,6" />
                    <Button Text="FINISH"
                            Command="{Binding CompleteWorkoutCommand}"
                            BackgroundColor="{StaticResource Accent}"
                            TextColor="{StaticResource AccentText}"
                            FontAttributes="Bold"
                            CornerRadius="10"
                            FontSize="14"
                            Padding="16,8" />
                </HorizontalStackLayout>
            </Grid>

            <!-- Tags + workout notes row -->
            <VerticalStackLayout Grid.Row="1"
                                 BackgroundColor="{StaticResource AppDarkSurface}"
                                 Padding="12,4,12,10"
                                 Spacing="6">

                <!-- Tag chips + add button -->
                <FlexLayout Wrap="Wrap"
                            Direction="Row"
                            AlignItems="Center"
                            BindableLayout.ItemsSource="{Binding WorkoutTags}">
                    <BindableLayout.ItemTemplate>
                        <DataTemplate x:DataType="vm:TagChipViewModel">
                            <Border StrokeShape="RoundRectangle 8"
                                    BackgroundColor="#1A3020"
                                    Stroke="#2D5C3A"
                                    StrokeThickness="1"
                                    Padding="8,3"
                                    Margin="0,0,4,4">
                                <HorizontalStackLayout Spacing="5">
                                    <Label Text="{Binding Name}"
                                           FontSize="12"
                                           TextColor="{StaticResource Accent}"
                                           VerticalOptions="Center" />
                                    <Label Text="&#xE5CD;"
                                           FontFamily="MaterialIcons"
                                           FontSize="10"
                                           TextColor="#888888"
                                           VerticalOptions="Center">
                                        <Label.GestureRecognizers>
                                            <TapGestureRecognizer Command="{Binding RemoveCommand}" />
                                        </Label.GestureRecognizers>
                                    </Label>
                                </HorizontalStackLayout>
                            </Border>
                        </DataTemplate>
                    </BindableLayout.ItemTemplate>
                </FlexLayout>

                <Button Text="+ Tag"
                        Command="{Binding AddTagCommand}"
                        BackgroundColor="Transparent"
                        TextColor="#888888"
                        BorderColor="#444444"
                        BorderWidth="1"
                        CornerRadius="6"
                        FontSize="12"
                        Padding="8,3"
                        HorizontalOptions="Start" />

                <!-- Workout-level notes -->
                <Entry Text="{Binding WorkoutNotes}"
                       Placeholder="Workout notes…"
                       PlaceholderColor="#555555"
                       TextColor="#CCCCCC"
                       BackgroundColor="Transparent"
                       FontSize="13" />
            </VerticalStackLayout>

            <BoxView Grid.Row="2" Color="{StaticResource AppDarkBorder}" />
        </Grid>

        <!-- ── Exercise list ── -->
        <ScrollView Grid.Row="1">
            <VerticalStackLayout Spacing="0" Padding="16,12,16,100">

                <!-- Activity indicator -->
                <ActivityIndicator IsRunning="{Binding IsBusy}"
                                   IsVisible="{Binding IsBusy}"
                                   Color="{StaticResource Accent}"
                                   HorizontalOptions="Center"
                                   Margin="0,40" />

                <!-- Exercises using BindableLayout (avoids CollectionView-in-ScrollView issues) -->
                <VerticalStackLayout BindableLayout.ItemsSource="{Binding Exercises}" Spacing="12">
                    <BindableLayout.ItemTemplate>
                        <DataTemplate x:DataType="vm:WorkoutExerciseViewModel">

                            <Border BackgroundColor="{StaticResource AppDarkSurface}"
                                    Stroke="{StaticResource AppDarkBorder}"
                                    StrokeThickness="1"
                                    StrokeShape="RoundRectangle 14"
                                    Padding="14,12">

                                <VerticalStackLayout Spacing="10">

                                    <!-- Exercise header -->
                                    <Grid ColumnDefinitions="*,Auto">
                                        <Label Grid.Column="0"
                                               Text="{Binding ExerciseName}"
                                               TextColor="White"
                                               FontSize="16"
                                               FontAttributes="Bold"
                                               VerticalOptions="Center" />
                                        <Button Grid.Column="1"
                                                Text="&#xE5CD;"
                                                FontFamily="MaterialIcons"
                                                Command="{Binding RemoveCommand}"
                                                BackgroundColor="Transparent"
                                                TextColor="#555555"
                                                FontSize="15"
                                                Padding="8,4"
                                                MinimumWidthRequest="36"
                                                MinimumHeightRequest="36" />
                                    </Grid>

                                    <!-- Exercise-level notes -->
                                    <Entry Text="{Binding ExerciseNotes, Mode=TwoWay}"
                                           Placeholder="Exercise notes…"
                                           PlaceholderColor="#666666"
                                           TextColor="#CCCCCC"
                                           BackgroundColor="#1E1E1E"
                                           FontSize="12" />

                                    <!-- Sets column header -->
                                    <Grid ColumnDefinitions="36,*,80,52,60" ColumnSpacing="6" Padding="4,0,0,0">
                                        <Label Grid.Column="0" Text="SET" TextColor="#666666" FontSize="11" FontAttributes="Bold" CharacterSpacing="1" />
                                        <Label Grid.Column="1" Text="WEIGHT" TextColor="#666666" FontSize="11" FontAttributes="Bold" CharacterSpacing="1" HorizontalOptions="Center" />
                                        <Label Grid.Column="2" Text="REPS" TextColor="#666666" FontSize="11" FontAttributes="Bold" CharacterSpacing="1" HorizontalOptions="Center" />
                                        <Label Grid.Column="3" TextColor="#666666" FontSize="11" />
                                        <Label Grid.Column="4" Text="TEMPO" TextColor="#666666" FontSize="11" FontAttributes="Bold" CharacterSpacing="1" HorizontalOptions="Center" />
                                    </Grid>

                                    <!-- Sets -->
                                    <VerticalStackLayout BindableLayout.ItemsSource="{Binding Sets}" Spacing="6">
                                        <BindableLayout.ItemTemplate>
                                            <DataTemplate x:DataType="vm:WorkoutSetViewModel">

                                                <SwipeView>
                                                    <SwipeView.RightItems>
                                                        <SwipeItems>
                                                            <SwipeItem Text="Duplicate"
                                                                       BackgroundColor="#1A3A5C"
                                                                       Command="{Binding DuplicateCommand}" />
                                                        </SwipeItems>
                                                    </SwipeView.RightItems>

                                                    <VerticalStackLayout Spacing="2">

                                                        <Grid ColumnDefinitions="36,*,80,52,60"
                                                              ColumnSpacing="6"
                                                              Padding="4,2,0,2">

                                                            <!-- Set number -->
                                                            <Label Grid.Column="0"
                                                                   Text="{Binding SetNumber}"
                                                                   TextColor="#888888"
                                                                   FontSize="14"
                                                                   VerticalOptions="Center"
                                                                   HorizontalOptions="Center" />

                                                        <!-- Weight: [−] entry [+] -->
                                                        <Grid Grid.Column="1"
                                                              ColumnDefinitions="30,*,30"
                                                              ColumnSpacing="2">
                                                            <Button Grid.Column="0"
                                                                    Text="−"
                                                                    Command="{Binding DecrementWeightCommand}"
                                                                    BackgroundColor="#2A2A2A"
                                                                    TextColor="White"
                                                                    FontSize="18"
                                                                    CornerRadius="8"
                                                                    Padding="0"
                                                                    HeightRequest="40"
                                                                    WidthRequest="30" />
                                                            <Entry Grid.Column="1"
                                                                   Text="{Binding WeightText}"
                                                                   Keyboard="Numeric"
                                                                   TextColor="White"
                                                                   BackgroundColor="#222222"
                                                                   HorizontalTextAlignment="Center"
                                                                   FontSize="14"
                                                                   HeightRequest="40" />
                                                            <Button Grid.Column="2"
                                                                    Text="+"
                                                                    Command="{Binding IncrementWeightCommand}"
                                                                    BackgroundColor="#2A2A2A"
                                                                    TextColor="White"
                                                                    FontSize="18"
                                                                    CornerRadius="8"
                                                                    Padding="0"
                                                                    HeightRequest="40"
                                                                    WidthRequest="30" />
                                                        </Grid>

                                                        <!-- Reps entry -->
                                                        <Entry Grid.Column="2"
                                                               Text="{Binding RepsText}"
                                                               Keyboard="Numeric"
                                                               TextColor="White"
                                                               BackgroundColor="#222222"
                                                               HorizontalTextAlignment="Center"
                                                               FontSize="14"
                                                               HeightRequest="40" />

                                                        <!-- Complete button -->
                                                        <Button Grid.Column="3"
                                                                Text="&#xE5CA;"
                                                                FontFamily="MaterialIcons"
                                                                Command="{Binding CompleteSetCommand}"
                                                                TextColor="White"
                                                                FontSize="18"
                                                                CornerRadius="8"
                                                                HeightRequest="40"
                                                                Padding="0">
                                                            <Button.Triggers>
                                                                <DataTrigger TargetType="Button"
                                                                             Binding="{Binding IsCompleted}"
                                                                             Value="False">
                                                                    <Setter Property="BackgroundColor" Value="#2A2A2A" />
                                                                </DataTrigger>
                                                                <DataTrigger TargetType="Button"
                                                                             Binding="{Binding IsCompleted}"
                                                                             Value="True">
                                                                    <Setter Property="BackgroundColor" Value="#2D7A3A" />
                                                                </DataTrigger>
                                                            </Button.Triggers>
                                                        </Button>

                                                        <!-- Tempo dropdown -->
                                                        <Border Grid.Column="4"
                                                                StrokeShape="RoundRectangle 6"
                                                                Stroke="Transparent"
                                                                BackgroundColor="#222222"
                                                                HeightRequest="40"
                                                                Padding="0">
                                                            <Border.GestureRecognizers>
                                                                <TapGestureRecognizer Command="{Binding SelectTempoCommand}" />
                                                            </Border.GestureRecognizers>
                                                            <Label Text="{Binding TempoDisplay}"
                                                                   TextColor="#AAAAAA"
                                                                   FontSize="12"
                                                                   HorizontalOptions="Center"
                                                                   VerticalOptions="Center"
                                                                   HorizontalTextAlignment="Center" />
                                                        </Border>

                                                    </Grid>

                                                    </VerticalStackLayout>
                                                </SwipeView>

                                            </DataTemplate>
                                        </BindableLayout.ItemTemplate>
                                    </VerticalStackLayout>

                                    <!-- Add Set button -->
                                    <Button Text="+ Add Set"
                                            Command="{Binding AddSetCommand}"
                                            BackgroundColor="Transparent"
                                            TextColor="{StaticResource Accent}"
                                            BorderColor="#333333"
                                            BorderWidth="1"
                                            CornerRadius="8"
                                            HeightRequest="40"
                                            FontSize="14" />

                                </VerticalStackLayout>
                            </Border>

                        </DataTemplate>
                    </BindableLayout.ItemTemplate>
                </VerticalStackLayout>

                <!-- Add Exercise button -->
                <Button Text="+ ADD EXERCISE"
                        Command="{Binding RequestAddExerciseCommand}"
                        BackgroundColor="{StaticResource AppDarkSurface}"
                        TextColor="{StaticResource Accent}"
                        BorderColor="{StaticResource Accent}"
                        BorderWidth="1"
                        CornerRadius="14"
                        HeightRequest="54"
                        FontAttributes="Bold"
                        FontSize="15"
                        CharacterSpacing="1"
                        Margin="0,12,0,0" />

                <!-- Empty state (shown when not loading and no exercises added) -->
                <Label Text="Tap &quot;+ ADD EXERCISE&quot; to start logging"
                       TextColor="#555555"
                       FontSize="14"
                       HorizontalOptions="Center"
                       Margin="0,8,0,0"
                       IsVisible="False">
                    <Label.Triggers>
                        <MultiTrigger TargetType="Label">
                            <MultiTrigger.Conditions>
                                <BindingCondition Binding="{Binding IsBusy}" Value="False" />
                                <BindingCondition Binding="{Binding Exercises.Count}" Value="0" />
                            </MultiTrigger.Conditions>
                            <Setter Property="IsVisible" Value="True" />
                        </MultiTrigger>
                    </Label.Triggers>
                </Label>

            </VerticalStackLayout>
        </ScrollView>

        <!-- ── Rest Timer bar ── -->
        <Grid Grid.Row="2"
              IsVisible="{Binding IsRestTimerVisible}"
              BackgroundColor="{StaticResource AppDarkSurface}"
              RowDefinitions="1.5,Auto">
            <BoxView Grid.Row="0" Color="{StaticResource Accent}" />
            <Grid Grid.Row="1" ColumnDefinitions="*,Auto" Padding="20,10">
                <VerticalStackLayout Grid.Column="0" Spacing="0" VerticalOptions="Center">
                    <Label Text="REST"
                           TextColor="#888888"
                           FontSize="11"
                           FontAttributes="Bold"
                           CharacterSpacing="2" />
                    <Label Text="{Binding RestTimeDisplay}"
                           TextColor="{StaticResource Accent}"
                           FontSize="30"
                           FontAttributes="Bold"
                           FontFamily="OpenSansRegular" />
                </VerticalStackLayout>
                <Button Grid.Column="1"
                        Text="SKIP"
                        Command="{Binding SkipRestCommand}"
                        BackgroundColor="Transparent"
                        TextColor="#888888"
                        FontSize="14"
                        FontAttributes="Bold"
                        Padding="12,8" />
            </Grid>
        </Grid>

    </Grid>
</ContentPage>
